# SoloLab - 反向代理配置

:80 {
	# 前端
	handle {
		reverse_proxy frontend:3000
	}

	# 后端 API
	handle /api/* {
		reverse_proxy backend:8000
	}

	# LiteLLM 代理（内部）
	handle /litellm/* {
		reverse_proxy litellm:4000
	}
}
