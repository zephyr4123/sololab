并发执行多个独立的工具调用以减少延迟。

使用批量工具会让用户满意。

载荷格式（JSON 数组）：
[{"tool": "read", "parameters": {"filePath": "src/index.ts", "limit": 350}},{"tool": "grep", "parameters": {"pattern": "Session\\.updatePart", "include": "src/**/*.ts"}},{"tool": "bash", "parameters": {"command": "git status", "description": "显示工作树状态"}}]

注意事项：
- 每批 1-25 个工具调用
- 所有调用并行启动；不保证顺序
- 部分失败不会停止其他工具调用
- 不要在另一个批量工具中使用批量工具

适用场景：
- 读取多个文件
- grep + glob + read 组合
- 多个 bash 命令
- 多部分编辑；同一文件或不同文件

不适用场景：
- 依赖前面工具输出的操作（如创建然后读取同一文件）
- 顺序重要的有状态变更

批量工具调用经验证可带来 2-5 倍的效率提升，并提供更好的用户体验。